package com.ie.evalos.domain;

/** A concern recorded against an expert. Stored in expert.performance_flags. */
public enum PerformanceFlag {

	SLOW_RESPONSE,
	QUALITY_ISSUE,
	DECLINED_CASES,
	CLIENT_COMPLAINT
}
