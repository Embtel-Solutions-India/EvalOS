package com.ie.evalos.domain;

/** Where the lead came from. Captured from GHL, never set by EvalOS. */
public enum SourceChannel {

	WEBSITE,
	GOOGLE_ADS,
	EMAIL_CAMPAIGN,
	LINKEDIN,
	INSTAGRAM,
	FACEBOOK,
	WHATSAPP,
	REFERRAL,
	PARTNER
}
