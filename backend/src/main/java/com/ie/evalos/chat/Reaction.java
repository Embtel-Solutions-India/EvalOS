package com.ie.evalos.chat;

/** The six reactions a message can carry (Unit 57 §2). Names, not emoji, so they travel safely in URLs. */
public enum Reaction {
	THUMBS_UP, HEART, LAUGH, CELEBRATE, SURPRISED, THANKS
}
